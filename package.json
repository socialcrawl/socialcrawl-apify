{
  "name": "socialcrawl-apify",
  "version": "1.5.0",
  "description": "Apify Actor for SocialCrawl — one API key for 65 social, commerce, app-store, review, places & travel, finance, jobs and web-research platforms across 575 endpoints, plus universal cross-platform search, Prism composites, scheduled monitors, audience-filtered cohort queries, `include=…` row joins billed only for the rows they fill, full per-endpoint credit pricing with a dispute-grade credit ledger, and free self-documenting quickstart / endpoint-guide / agent-context actions, all behind one unified response envelope.",
  "type": "module",
  "engines": {
    "node": ">=18.0.0"
  },
  "scripts": {
    "start": "npm run start:prod",
    "start:prod": "node dist/main.js",
    "start:dev": "tsx src/main.ts",
    "build": "tsc",
    "type-check": "tsc --noEmit",
    "generate:data": "tsx scripts/generate-data.ts",
    "test": "vitest run"
  },
  "dependencies": {
    "apify": "^3.2.6"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2",
    "vitest": "^3.1.1"
  },
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/RidioDevelopment/socialcrawl-apify"
  }
}
